# Build output
/bin/
/dist/

# Test and coverage artifacts
*.out
coverage.txt
coverage.html
*.test

# Downloaded release archives (never commit binaries)
*.tar.gz
checksums.txt

# Editor and OS noise
.DS_Store
*.swp
*~
.idea/
.vscode/
.claude/settings.local.json

# Local Herdr verification scratch state
/.verify-plugin-tmp/

# Local secrets — never commit
config.toml
*.env
